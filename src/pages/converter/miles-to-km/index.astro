---
import ToolLayout from "../../../components/ToolLayout.astro";

export const tool = {
  title: "miles to km",
  description:
    "Convert miles (mi) to kilometers (km) instantly. Includes validation, accessible UX, result cards, quick table, and FAQ schema.",
  href: "/converter/miles-to-km/",
  order: 60,
};

const SITE_NAME = "MinimalDev";
const CANONICAL_PATH = "/converter/miles-to-km/";

const faqs = [
  {
    q: "What is the miles to km formula?",
    a: "kilometers = miles × 1.609344. (Because 1 mile = 1.609344 km exactly.)",
  },
  {
    q: "Is this conversion exact?",
    a: "The base relationship 1 mile = 1.609344 km is exact. Displayed results may be rounded.",
  },
  {
    q: "Can I convert kilometers to miles here?",
    a: "Yes. Switch the direction to “Kilometers → Miles”.",
  },
  {
    q: "Are my inputs stored?",
    a: "No. All calculations run locally in your browser.",
  },
];

const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((f) => ({
    "@type": "Question",
    name: f.q,
    acceptedAnswer: { "@type": "Answer", text: f.a },
  })),
};
---

<ToolLayout
  title={`miles to km Converter — ${SITE_NAME}`}
  description="Convert miles (mi) to kilometers (km) instantly, with an option to reverse (km → mi). Includes validation, accessible controls, result cards, quick conversions, and FAQ schema."
  canonicalPath={CANONICAL_PATH}
  h1="miles to km Converter"
  intro="Convert miles to kilometers (and kilometers to miles) using standard distance conversions."
  faq={faqs}
  categoryName="Converters"
>
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} />

  <div slot="tool" data-tool="miles-to-km">
    <div class="form" role="form" aria-describedby="m2k_privacy m2k_help">
      <div class="field">
        <label for="m2k_value">Value</label>
        <input
          id="m2k_value"
          type="number"
          inputmode="decimal"
          placeholder="e.g., 3.1"
          aria-describedby="m2k_help"
        />
        <p class="note" id="m2k_help" style="margin-top:6px;">
          Reference: <strong>1 mi = 1.609344 km</strong> (exact).
        </p>
      </div>

      <div class="field">
        <label for="m2k_mode">Direction</label>
        <select id="m2k_mode">
          <option value="mi_to_km" selected>Miles → Kilometers</option>
          <option value="km_to_mi">Kilometers → Miles</option>
        </select>
      </div>

      <div class="field">
        <label for="m2k_round">Rounding</label>
        <select id="m2k_round">
          <option value="2">2 decimals</option>
          <option value="3" selected>3 decimals</option>
          <option value="4">4 decimals</option>
          <option value="8">Up to 8 decimals</option>
        </select>
      </div>

      <div style="display:flex; gap:10px; flex-wrap:wrap; align-items:center;">
        <button class="btn" type="button" id="m2k_calc">Convert</button>
        <button class="btn secondary" type="button" id="m2k_swap" aria-label="Swap direction">Swap</button>
        <button class="btn secondary" type="button" id="m2k_clear">Clear</button>
      </div>

      <div class="result" id="m2k_error" role="alert" style="display:none; margin-top:12px;"></div>

      <div class="result" id="m2k_result" role="status" style="margin-top:12px;">
        Result will appear here.
      </div>

      <div class="result" id="m2k_cards" style="display:none; margin-top:12px;"></div>

      <div style="margin-top:16px; overflow:auto;">
        <table style="width:100%; border-collapse:collapse;">
          <thead>
            <tr>
              <th scope="col">Quick conversions</th>
              <th scope="col">Result</th>
            </tr>
          </thead>
          <tbody id="m2k_table"></tbody>
        </table>
      </div>

      <p class="note" id="m2k_privacy" style="margin-top:10px;">
        Privacy: runs locally in your browser. No inputs are stored or transmitted.
      </p>
    </div>
  </div>

  <div slot="how">
    <p class="note">
      <strong>mi → km:</strong> kilometers = miles × 1.609344<br/>
      <strong>km → mi:</strong> miles = kilometers ÷ 1.609344
    </p>
  </div>

  <div slot="examples">
    <ul>
      <li><strong>3.1 mi</strong> → <strong>4.989 km</strong> (approx.)</li>
      <li><strong>6.2 mi</strong> → <strong>9.978 km</strong> (approx.)</li>
      <li><strong>26.2 mi</strong> → <strong>42.195 km</strong> (marathon distance, approx. display)</li>
    </ul>
  </div>

  <div slot="related">
    <ul>
      <li><a href="/converter/km-to-miles/">km to miles</a></li>
      <li><a href="/converter/length/">Length Converter</a></li>
      <li><a href="/converter/speed-converter/">Speed Converter</a></li>
      <li><a href="/converter/">All Converters</a></li>
    </ul>
  </div>
</ToolLayout>

<script is:inline>
document.addEventListener("DOMContentLoaded", function () {
  const valueEl = document.getElementById("m2k_value");
  const modeEl = document.getElementById("m2k_mode");
  const roundEl = document.getElementById("m2k_round");
  const calcBtn = document.getElementById("m2k_calc");
  const swapBtn = document.getElementById("m2k_swap");
  const clearBtn = document.getElementById("m2k_clear");
  const errEl = document.getElementById("m2k_error");
  const resEl = document.getElementById("m2k_result");
  const cardsEl = document.getElementById("m2k_cards");
  const tbodyEl = document.getElementById("m2k_table");

  if (!valueEl || !modeEl || !roundEl || !calcBtn || !swapBtn || !clearBtn || !errEl || !resEl || !cardsEl || !tbodyEl) return;

  const MI_IN_KM = 1.609344; // exact

  function toNum(v) {
    const x = parseFloat(String(v));
    return Number.isFinite(x) ? x : NaN;
  }

  function clearError() {
    errEl.style.display = "none";
    errEl.textContent = "";
  }

  function showError(msg) {
    errEl.style.display = "block";
    errEl.textContent = msg;
  }

  function clampAbs(x, maxAbs) {
    if (!Number.isFinite(x)) return x;
    if (Math.abs(x) > maxAbs) return Math.sign(x) * maxAbs;
    return x;
  }

  function fmtFixed(x, decimals) {
    if (!Number.isFinite(x)) return "—";
    if (decimals === 8) {
      const r = Math.round(x * 1e8) / 1e8;
      return r.toString();
    }
    return x.toFixed(decimals);
  }

  function badge(text, kind) {
    const border = kind === "warn"
      ? "rgba(251,146,60,0.55)"
      : kind === "good"
      ? "rgba(34,197,94,0.55)"
      : "rgba(255,255,255,0.2)";
    return `<span style="display:inline-block; padding:2px 10px; border-radius:999px; border:1px solid ${border}; font-size:12px; font-weight:900;">${text}</span>`;
  }

  function compute() {
    clearError();
    cardsEl.style.display = "none";
    cardsEl.innerHTML = "";
    tbodyEl.innerHTML = "";

    const raw = toNum(valueEl.value);
    if (!Number.isFinite(raw)) {
      resEl.textContent = "Please enter a valid number.";
      return;
    }

    const v = clampAbs(raw, 1e18);
    if (v !== raw) {
      showError("Value is too large for reliable display. Using a clamped value for output.");
    }

    const mode = String(modeEl.value);
    const decimals = parseInt(String(roundEl.value), 10);

    let out, fromUnit, toUnit, factorNote;

    if (mode === "mi_to_km") {
      out = v * MI_IN_KM;
      fromUnit = "mi";
      toUnit = "km";
      factorNote = "× 1.609344";
    } else {
      out = v / MI_IN_KM;
      fromUnit = "km";
      toUnit = "mi";
      factorNote = "÷ 1.609344";
    }

    const outStr = fmtFixed(out, decimals);
    const inStr = fmtFixed(v, decimals === 8 ? 8 : Math.min(decimals, 6));

    resEl.innerHTML =
      "<strong>Result:</strong> " + outStr + " " + toUnit + "<br/>" +
      "<span class='note'>" + inStr + " " + fromUnit + " " + factorNote + " = " + outStr + " " + toUnit + "</span>";

    cardsEl.style.display = "block";
    cardsEl.innerHTML = `
      <div style="display:flex; gap:10px; flex-wrap:wrap;">
        ${badge("From: " + fromUnit, "neutral")}
        ${badge("To: " + toUnit, "neutral")}
        ${badge("Rounding: " + (decimals === 8 ? "up to 8" : decimals) + " dp", "neutral")}
        ${badge("Converted", "good")}
      </div>
    `;

    const presets = mode === "mi_to_km"
      ? [1, 3.1, 6.2, 13.1, 26.2]
      : [1, 5, 10, 21.0975, 42.195];

    for (const s0 of presets) {
      const s = s0;
      const o = mode === "mi_to_km" ? (s * MI_IN_KM) : (s / MI_IN_KM);
      const tr = document.createElement("tr");
      const td1 = document.createElement("td");
      const td2 = document.createElement("td");
      td1.textContent = fmtFixed(s, decimals === 8 ? 8 : Math.min(decimals, 4)) + " " + fromUnit;
      td2.textContent = fmtFixed(o, decimals) + " " + toUnit;
      tr.appendChild(td1);
      tr.appendChild(td2);
      tbodyEl.appendChild(tr);
    }
  }

  function reset() {
    clearError();
    valueEl.value = "";
    modeEl.value = "mi_to_km";
    roundEl.value = "3";
    resEl.textContent = "Result will appear here.";
    cardsEl.style.display = "none";
    cardsEl.innerHTML = "";
    tbodyEl.innerHTML = "";
  }

  calcBtn.addEventListener("click", compute);

  swapBtn.addEventListener("click", function () {
    modeEl.value = modeEl.value === "mi_to_km" ? "km_to_mi" : "mi_to_km";
    if (String(valueEl.value || "").trim()) compute();
  });

  clearBtn.addEventListener("click", reset);

  modeEl.addEventListener("change", function () {
    if (String(valueEl.value || "").trim()) compute();
  });

  roundEl.addEventListener("change", function () {
    if (String(valueEl.value || "").trim()) compute();
  });

  valueEl.addEventListener("keydown", function (e) {
    if ((e.ctrlKey || e.metaKey) && e.key === "Enter") compute();
  });

  valueEl.addEventListener("input", function () {
    if (!String(valueEl.value || "").trim()) {
      clearError();
      resEl.textContent = "Result will appear here.";
      cardsEl.style.display = "none";
      cardsEl.innerHTML = "";
      tbodyEl.innerHTML = "";
    }
  });
});
</script>