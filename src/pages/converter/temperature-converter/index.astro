---
import ToolLayout from "../../../components/ToolLayout.astro";

export const tool = {
  title: "Temperature Converter",
  description: "Convert Celsius, Fahrenheit, and Kelvin instantly (C to F, F to C, C to K).",
  href: "/converter/temperature-converter/",
  order: 30,
};

const faq = [
  {
    q: "Why is temperature conversion different from other unit converters?",
    a: "Temperature scales include offsets (such as +32 or +273.15) in addition to ratios. Unlike length or weight, you cannot convert using multiplication alone.",
  },
  {
    q: "Can I enter negative values?",
    a: "Yes. Negative temperatures such as −10°C or −40°F are handled correctly.",
  },
  {
    q: "Why do I sometimes see rounding?",
    a: "Some conversions produce long repeating decimals. The tool rounds results for readability while still using standard formulas.",
  },
  {
    q: "Does this tool store or transmit my data?",
    a: "No. All calculations run locally in your browser. No values are stored or sent to a server.",
  },
];
---

<ToolLayout
  title="Temperature Converter — MinimalDev"
  description="Free temperature converter for Celsius (°C), Fahrenheit (°F), and Kelvin (K). Convert C to F, F to C, and more with instant results."
  canonicalPath="/converter/temperature-converter/"
  h1="Temperature Converter"
  intro="Convert Celsius, Fahrenheit, and Kelvin instantly. Supports common conversions like C to F and F to C with live updates."
  faq={faq}
>
  <div slot="tool" data-tool="temperature-converter">
    <div class="form">
      <div class="field">
        <label for="tc_value">Value</label>
        <input id="tc_value" type="number" inputmode="decimal" placeholder="e.g., 36.6" />
      </div>

      <div class="field">
        <label for="tc_from_unit">From</label>
        <select id="tc_from_unit">
          <option value="c" selected>Celsius (°C)</option>
          <option value="f">Fahrenheit (°F)</option>
          <option value="k">Kelvin (K)</option>
        </select>
      </div>

      <div class="field">
        <label for="tc_to_unit">To</label>
        <select id="tc_to_unit">
          <option value="c">Celsius (°C)</option>
          <option value="f" selected>Fahrenheit (°F)</option>
          <option value="k">Kelvin (K)</option>
        </select>
      </div>

      <div style="display:flex; gap:10px; flex-wrap:wrap;">
        <button class="btn secondary" type="button" id="tc_swap">Swap</button>
        <button class="btn secondary" type="button" id="tc_clear">Clear</button>
      </div>

      <div class="result" id="tc_result">Result will appear here.</div>

      <p class="note" style="margin-top:8px;">
        Privacy: calculations run locally in your browser. No inputs are stored or sent to a server.
      </p>
    </div>
  </div>

  <div slot="how">
    <p class="note">
      Temperature conversion differs from most unit converters because it involves both a scale and an offset.
      For example, converting from Celsius to Fahrenheit requires multiplying and adding 32,
      while converting to Kelvin requires adding 273.15.
    </p>

    <p class="note">
      This tool converts values through Celsius internally when needed.
      It supports exact formulas for C ↔ F and C ↔ K conversions,
      ensuring mathematically correct results.
    </p>

    <ul>
      <li><strong>C → F:</strong> (°C × 9/5) + 32</li>
      <li><strong>F → C:</strong> (°F − 32) × 5/9</li>
      <li><strong>C → K:</strong> °C + 273.15</li>
      <li><strong>K → C:</strong> K − 273.15</li>
    </ul>

    <p class="note">
      If a result contains many decimal places, it is rounded for display clarity.
    </p>
  </div>

  <div slot="examples">
    <ul>
      <li><strong>0°C</strong> = <strong>32°F</strong> = <strong>273.15 K</strong></li>
      <li><strong>100°C</strong> = <strong>212°F</strong> = <strong>373.15 K</strong></li>
      <li><strong>98.6°F</strong> ≈ <strong>37°C</strong></li>
      <li><strong>300 K</strong> ≈ <strong>26.85°C</strong></li>
    </ul>
  </div>

  <div slot="related">
    <ul>
      <li><a href="/converter/length-converter/">Length Converter</a></li>
      <li><a href="/converter/weight-converter/">Weight Converter</a></li>
      <li><a href="/converter/">All Converters</a></li>
    </ul>
  </div>
</ToolLayout>

<script is:inline>
document.addEventListener("DOMContentLoaded", function () {
  const valueEl = document.getElementById("tc_value");
  const fromEl = document.getElementById("tc_from_unit");
  const toEl = document.getElementById("tc_to_unit");
  const swapBtn = document.getElementById("tc_swap");
  const clearBtn = document.getElementById("tc_clear");
  const resultEl = document.getElementById("tc_result");

  if (!valueEl || !fromEl || !toEl || !swapBtn || !clearBtn || !resultEl) return;

  function isFiniteNumber(x) {
    return typeof x === "number" && isFinite(x);
  }

  function fmt(n) {
    if (!isFiniteNumber(n)) return "";
    return n.toLocaleString(undefined, { maximumFractionDigits: 6 });
  }

  function toCelsius(val, unit) {
    if (unit === "c") return val;
    if (unit === "f") return (val - 32) * 5 / 9;
    if (unit === "k") return val - 273.15;
    return val;
  }

  function fromCelsius(val, unit) {
    if (unit === "c") return val;
    if (unit === "f") return (val * 9 / 5) + 32;
    if (unit === "k") return val + 273.15;
    return val;
  }

  function calc() {
    const raw = parseFloat(valueEl.value);
    const from = fromEl.value;
    const to = toEl.value;

    if (!isFiniteNumber(raw)) {
      resultEl.textContent = "Result will appear here.";
      return;
    }

    const c = toCelsius(raw, from);
    const out = fromCelsius(c, to);

    const primary = fmt(out);
    const approx = String(primary) !== String(out) ? " (approx.)" : "";

    resultEl.innerHTML =
      "<strong>" + primary + " " + to.toUpperCase() + "</strong>" + approx +
      "<div class='note' style='margin-top:6px;'>" +
      fmt(raw) + " " + from.toUpperCase() +
      " = " + primary + " " + to.toUpperCase() +
      "</div>";
  }

  valueEl.addEventListener("input", calc);
  fromEl.addEventListener("change", calc);
  toEl.addEventListener("change", calc);

  swapBtn.addEventListener("click", function () {
    const temp = fromEl.value;
    fromEl.value = toEl.value;
    toEl.value = temp;
    calc();
  });

  clearBtn.addEventListener("click", function () {
    valueEl.value = "";
    resultEl.textContent = "Result will appear here.";
  });

  calc();
});
</script>