---
import ToolLayout from "../../../components/ToolLayout.astro";

export const tool = {
  title: "Compound Interest Calculator",
  description:
    "Calculate compound interest with optional monthly contributions. Estimate future value, total contributions, and total interest over time.",
  href: "/finance/compound-interest-calculator/",
  order: 20,
};

const faq = [
  {
    q: "Does this assume monthly compounding?",
    a: "Yes. This calculator compounds monthly and (optionally) adds a monthly contribution each month.",
  },
  {
    q: "Are monthly contributions added at the beginning or end of the month?",
    a: "This tool adds the monthly contribution at the end of each month (after that month’s interest is applied).",
  },
  {
    q: "What if the interest rate is 0%?",
    a: "If the rate is 0%, the result is simply principal plus total monthly contributions.",
  },
  {
    q: "Why does my brokerage projection differ?",
    a: "Providers can assume different compounding frequency, contribution timing, fees, taxes, and rounding. This is a simplified estimate for planning.",
  },
  {
    q: "Does this include inflation or taxes?",
    a: "No. For purchasing-power adjustments, use the Inflation Calculator. Taxes and fees are not included.",
  },
];
---

<ToolLayout
  title="Compound Interest Calculator — MinimalDev"
  description="Calculate compound interest with optional monthly contributions. View future value, total contributions, and total interest with a year-by-year breakdown."
  canonicalPath="/finance/compound-interest-calculator/"
  h1="Compound Interest Calculator"
  intro="Estimate investment growth with compound interest and optional monthly contributions. Includes a year-by-year table for planning."
  faq={faq}
>
  <div slot="tool" data-tool="compound-interest-calculator">
    <div class="form">
      <div class="field">
        <label for="ci_principal">Initial investment</label>
        <input id="ci_principal" type="number" inputmode="decimal" placeholder="e.g., 10000" />
      </div>

      <div class="field">
        <label for="ci_rate">Annual interest rate (%)</label>
        <input id="ci_rate" type="number" inputmode="decimal" placeholder="e.g., 7" />
        <p class="note" style="margin-top:6px;">
          Use expected annual return (e.g., 7). This tool compounds monthly.
        </p>
      </div>

      <div class="field">
        <label for="ci_years">Investment period (years)</label>
        <input id="ci_years" type="number" inputmode="numeric" placeholder="e.g., 10" />
      </div>

      <div class="field">
        <label for="ci_monthly">Monthly contribution (optional)</label>
        <input id="ci_monthly" type="number" inputmode="decimal" placeholder="e.g., 200" />
        <p class="note" style="margin-top:6px;">
          Assumption: contribution is added at the end of each month.
        </p>
      </div>

      <div style="display:flex; gap:10px; flex-wrap:wrap;">
        <button id="ci_calc" class="btn" type="button">Calculate</button>
        <button id="ci_clear" class="btn secondary" type="button">Clear</button>
      </div>

      <div id="ci_summary" class="result" style="margin-top:12px;">Result will appear here.</div>

      <div style="margin-top:16px; overflow:auto;">
        <table style="width:100%; border-collapse:collapse; font-size:14px;">
          <thead>
            <tr>
              <th>Year</th>
              <th>Total value</th>
              <th>Total contributions</th>
              <th>Total interest</th>
            </tr>
          </thead>
          <tbody id="ci_tbody"></tbody>
        </table>
      </div>

      <p class="note" style="margin-top:10px;">
        Informational only. Results are estimates and don’t include taxes, fees, or inflation.
      </p>
    </div>
  </div>

  <div slot="how">
    <p class="note">
      Compound interest grows as interest is earned on both the original principal and previously earned interest.
      This calculator uses <strong>monthly compounding</strong> and optionally adds a monthly contribution.
    </p>

    <p class="note">
      If there are no contributions, a common reference formula is:<br/>
      <strong>A = P × (1 + r/n)^(n×t)</strong><br/>
      In this tool, we model growth month-by-month (n = 12), which also makes it easy to include monthly contributions.
    </p>

    <p class="note">
      The table below shows a year-by-year snapshot: total value, how much you contributed, and how much came from interest.
    </p>
  </div>

  <div slot="examples">
    <ul>
      <li>$10,000 initial, 7% annually, 10 years, $0 monthly → compare against the no-contribution scenario</li>
      <li>$5,000 initial, 6% annually, 20 years, $200 monthly → see how contributions dominate early growth</li>
      <li>Try changing years from 10 → 30 to see the compounding effect over longer periods</li>
    </ul>
  </div>

  <div slot="related">
    <ul>
      <li><a href="/finance/future-value-calculator/">Future Value Calculator</a></li>
      <li><a href="/finance/savings-calculator/">Savings Calculator</a></li>
      <li><a href="/finance/inflation-calculator/">Inflation Calculator</a></li>
      <li><a href="/finance/retirement-calculator/">Retirement Calculator</a></li>
      <li><a href="/finance/">All Finance Calculators</a></li>
    </ul>
  </div>
</ToolLayout>

<script is:inline>
document.addEventListener("DOMContentLoaded", function () {
  const principalEl = document.getElementById("ci_principal");
  const rateEl = document.getElementById("ci_rate");
  const yearsEl = document.getElementById("ci_years");
  const monthlyEl = document.getElementById("ci_monthly");
  const calcBtn = document.getElementById("ci_calc");
  const clearBtn = document.getElementById("ci_clear");
  const summaryEl = document.getElementById("ci_summary");
  const tbodyEl = document.getElementById("ci_tbody");

  if (!principalEl || !rateEl || !yearsEl || !monthlyEl || !calcBtn || !clearBtn || !summaryEl || !tbodyEl) return;

  function toNum(v) {
    const x = parseFloat(String(v));
    return isFinite(x) ? x : NaN;
  }

  function money(v) {
    if (!isFinite(v)) return "$0.00";
    return v.toLocaleString(undefined, { style: "currency", currency: "USD" });
  }

  calcBtn.addEventListener("click", function () {
    const P0 = toNum(principalEl.value);
    const annualPct = toNum(rateEl.value);
    const years = toNum(yearsEl.value);
    const monthly = String(monthlyEl.value).trim() === "" ? 0 : toNum(monthlyEl.value);

    if (!isFinite(P0) || P0 < 0) {
      summaryEl.textContent = "Please enter a valid initial investment (>= 0).";
      tbodyEl.innerHTML = "";
      return;
    }
    if (!isFinite(annualPct) || annualPct < 0) {
      summaryEl.textContent = "Please enter a valid annual rate (>= 0).";
      tbodyEl.innerHTML = "";
      return;
    }
    if (!isFinite(years) || years <= 0) {
      summaryEl.textContent = "Please enter a valid investment period in years (> 0).";
      tbodyEl.innerHTML = "";
      return;
    }
    if (!isFinite(monthly) || monthly < 0) {
      summaryEl.textContent = "Please enter a valid monthly contribution (>= 0).";
      tbodyEl.innerHTML = "";
      return;
    }

    const totalMonths = Math.round(years * 12);
    const r = (annualPct / 100) / 12;

    let balance = P0;
    let totalContrib = P0;
    let totalInterest = 0;

    tbodyEl.innerHTML = "";

    // Build year-by-year snapshot by simulating month-by-month
    for (let month = 1; month <= totalMonths; month++) {
      // Apply interest for the month
      if (r !== 0) balance = balance * (1 + r);

      // Contribution at end of month (assumption)
      if (monthly > 0) {
        balance += monthly;
        totalContrib += monthly;
      }

      // At each year boundary, output a row
      if (month % 12 === 0 || month === totalMonths) {
        const year = Math.ceil(month / 12);
        totalInterest = balance - totalContrib;

        const row =
          "<tr>" +
            "<td>" + year + "</td>" +
            "<td>" + money(balance) + "</td>" +
            "<td>" + money(totalContrib) + "</td>" +
            "<td>" + money(totalInterest) + "</td>" +
          "</tr>";
        tbodyEl.insertAdjacentHTML("beforeend", row);
      }
    }

    totalInterest = balance - totalContrib;

    summaryEl.innerHTML =
      "<strong>Final value:</strong> " + money(balance) + "<br/>" +
      "<strong>Total contributions:</strong> " + money(totalContrib) + "<br/>" +
      "<strong>Total interest earned:</strong> " + money(totalInterest);
  });

  clearBtn.addEventListener("click", function () {
    principalEl.value = "";
    rateEl.value = "";
    yearsEl.value = "";
    monthlyEl.value = "";
    summaryEl.textContent = "Result will appear here.";
    tbodyEl.innerHTML = "";
  });
});
</script>